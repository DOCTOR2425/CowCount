<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:viewModels="clr-namespace:CowCount.ViewModels">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="BarnsListTemplate.xaml" />
        <ResourceDictionary Source="SectionListTemplate.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <DataTemplate DataType="{x:Type viewModels:MainViewModel}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="100" MaxWidth="120" Width="0.3*" />
                <ColumnDefinition MinWidth="100" MaxWidth="180" Width="0.3*" />
                <ColumnDefinition MinWidth="100" MaxWidth="150" Width="0.3*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    BorderThickness="1"
                    BorderBrush="Black">
                <ContentControl Content="{Binding BarnsListViewModel}" />
            </Border>

            <Border Grid.Column="1"
                    BorderThickness="1"
                    BorderBrush="Black">
                <ContentControl Content="{Binding SectionListViewModel}" />
            </Border>
        </Grid>
    </DataTemplate>
</ResourceDictionary>