<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:viewModels="clr-namespace:CowCount.ViewModels">

    <DataTemplate DataType="{x:Type viewModels:BarnsListViewModel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0"
                   Content="Коровники"/>

            <Button Grid.Row="1"
                    Content="Добавить коровник"/>

            <ListView x:Name="barnsListView"
                      Grid.Row="2"
                      SelectionMode="Single"
                      ItemsSource="{Binding Barns}"
                      SelectedItem="{Binding SelectedBarn}">
            </ListView>
        </Grid>
    </DataTemplate>
</ResourceDictionary>